class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        l = []
        l2 = sorted(nums)
        for i in range(0, len(l2)):
            if l2[i] == target:
                l.append(i)
        
        return l

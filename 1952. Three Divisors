class Solution:
    def isThree(self, n: int) -> bool:
        sum = 0
        for i in range(1, n + 1):
            if n % i == 0:
                sum += 1
        
        if sum == 3:
            return 1 == 1
        
        else:
            return 1 != 1

class Solution:
    def removePalindromeSub(self, s: str) -> int:
        l = list(s)
        l2 = []
        for i in range(0, len(l)):
            l2.append(l[len(l)-i-1])
        
        if l == l2:
            return 1
        
        else:
            return 2

class Solution:
    def countSeniors(self, details: List[str]) -> int:
        sum = 0
        for i in details:
            if int(i[11:13]) > 60:
                sum = sum +1
        
        return sum

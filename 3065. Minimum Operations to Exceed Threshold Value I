class Solution:
    def minOperations(self, nums: List[int], k: int) -> int:
        sum = 0
        for i in range(0, len(nums)):
            if nums[i] < k:
                sum += 1
        
        return sum

class Solution:
    def prefixCount(self, words: List[str], pref: str) -> int:
        sum = 0
        for i in words:
            if i[:len(pref)] == pref:
                sum = sum + 1

        return sum

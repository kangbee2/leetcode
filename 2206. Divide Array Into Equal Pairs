class Solution:
    def divideArray(self, nums: List[int]) -> bool:
        sum = 0
        for i in range(0, len(nums)):
            if nums.count(nums[i]) % 2 != 0:
                sum += 1
        
        if sum != 0:
            return 1 != 1
        else:
            return 1 == 1

class Solution:
    def numOfStrings(self, patterns: List[str], word: str) -> int:
        sum = 0
        for i in patterns:
            if i in word:
                sum = sum + 1
        
        return sum

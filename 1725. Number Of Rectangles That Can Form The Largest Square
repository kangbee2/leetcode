class Solution:
    def countGoodRectangles(self, rectangles: List[List[int]]) -> int:
        l = []
        for i in range(0, len(rectangles)):
            l.append(min(rectangles[i]))
        
        return l.count(max(l))

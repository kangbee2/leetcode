import sys
sys.set_int_max_str_digits(10000)


class Solution:
    def addStrings(self, num1: str, num2: str) -> str:
        if 1 <= len(num1) and len(num2) <= 10000:
            return str(int(num1) + int(num2))
            


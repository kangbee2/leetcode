class Solution:
    def sumOfSquares(self, nums: List[int]) -> int:
        sum = 0
        for i in range(0, len(nums)):
            if len(nums) % (i+1) == 0:
                sum = sum + nums[i] * nums[i]
        
        return sum

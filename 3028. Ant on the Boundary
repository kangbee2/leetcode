class Solution:
    def returnToBoundaryCount(self, nums: List[int]) -> int:
        s = 0
        sum = 0
        for i in range(0, len(nums)):
            sum = sum + nums[i]
            if sum == 0:
                s += 1
        
        return s
